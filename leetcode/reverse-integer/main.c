#include "stdio.h"
#include "stdlib.h"

int reverse(int x)
{
    char str[12] = {0}; // 32-bit number max length is 10 + sign

    // convert integer to string
    sprintf(str, "%d", x);

    return 0;
}

int main()
{
    return 0;
}